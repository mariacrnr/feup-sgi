import {CGFnurbsSurface, CGFnurbsObject } from '../lib/CGF.js';

/**
 * MyAnimation
 * @constructor
 */
export class MyAnimation {
	constructor(scene) {
        this.scene = scene;
	}

    update(t){}

    apply(){}
}